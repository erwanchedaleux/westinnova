# Don't put config.yml in git, unless you're absolutely sure that all sensitive
# info (database credentials, mail settings) are _only_ in config_local.yml
app/config/config.yml

# Usually we don't put 'uploaded files' into git either.
files/

# Modify this, only if you've changed the default folder in .bolt.yml
public/bolt-public/

# -----------------------------------------------------------------------------

# Config files with '_local' should *never* go into git
app/config/*_local.yml

# SQLite databases should not go into git
app/database/*.db

cache/
thumbs/
vendor/
!app/resources/front/js/vendor/

# NPM, Gulp and Grunt stuff
node_modules
htmllint-report.xml
bower_components
todo.md
.sass-cache
npm-debug.log
yarn-error.log
.sass-cache
yarn.lock
npm-debug.log
app/resources/front/img/img_not_optimized
*.js.map
*.js.twig.map

# File-system cruft and temporary files
*.gz
*.sublime-*
*.zip
.*.swp
._*
.buildpath
.DS_Store
.DS_Store?
.AppleDouble
.LSOverride
.idea/
.vscode
.project
.swp
.Trashes
.vagrant*
/bolt.log
/tags
__*
php-cs-fixer.phar
scrutinizer.phar
composer.phar
Thumbs.db
Vagrantfile
